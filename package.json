{
  "name": "citrea",
  "version": "1.0.0",
  "description": "Smart contracts for Citrea network",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat ignition deploy ignition/modules/BIP110Bet.ts --network citrea"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
